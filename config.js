exports.DATABASE_URL = process.env.DATABASE_URL ||
                       global.DATABASE_URL ||
                      'mongodb://localhost/ofl';
exports.PORT = process.env.PORT || 8080;



//$.ajax({url: }) //order dosn't matter. 
//$.get 
//google ajax jquery get and jquery post, for del have to use ajax, as well as put uses ajax 
